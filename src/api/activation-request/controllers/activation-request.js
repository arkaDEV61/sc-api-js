'use strict';

/**
 * activation-request controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::activation-request.activation-request');
